import { ViewEditorDirective } from './view-editor.directive';

describe('ViewEditorDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewEditorDirective();
    expect(directive).toBeTruthy();
  });
});
